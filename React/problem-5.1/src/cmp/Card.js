function Card(props) {
  // const{todoTitle, TodoDes} =props;
  return (
    <div>
      <h1>this is ok</h1>
      <p>{props.todoTitle}</p>
      <p>{props.TodoDes}</p>
      <p>{props.todoAdd}</p>
      <h2>test two is fixxed</h2>
    </div>
  );
}

export default Card;
